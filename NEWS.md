# offsetpointbc 1.1.1
* updated offset_points_within_boundary() param definitions for rand_angle_min and rand_angle_max

# offsetpointbc 1.1.0

* added column showing distance point was offset by.

* includes warning if at least one point is offset or nudged back within 50m of original point.

* updated summary output to only appear if any points were offset beyond their original boundary; otherwise shows as succesfully run.

# offsetpointbc 1.0.0

* Initial version.
